#!/bin/bash

set -eo pipefail

cd $(dirname "${BASH_SOURCE[0]}")/..

stack=$1
if [[ ! -f ${stack}.json ]]; then
  >&2 echo "No such stack: ${stack}"
  exit 1
fi

docker run --rm -v "$(pwd)/..:/go/src/github.com/sclevine/packs" "packs/${stack}-test" github.com/sclevine/packs/cf/app
